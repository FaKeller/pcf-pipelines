---
# this task

platform: linux

image_resource:
  type: docker-image
  source: {repository: czero/cflinuxfs2}

params:
  OPSMAN_DOMAIN_OR_IP_ADDRESS:
  OPS_MGR_USR:
  OPS_MGR_PWD:
  PIVNET_API_TOKEN:
  NO_PROXY:
  OM_IP:
  IAAS:
  S3_BUCKET_NAME:
  S3_ENDPOINT:
  S3_PATH_PREFIX:

inputs:
  - name: pivnet-product
  - name: pcf-pipelines

run:
  path: pcf-pipelines/tasks-offline/upload-product-and-stemcell/task.sh
